<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.lsj.tingshu.user.mapper.UserInfoMapper">

	<resultMap id="userInfoMap" type="com.lsj.tingshu.model.user.UserInfo" autoMapping="true">
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="columns">
		id,phone,password,wx_open_id,nickname,avatar_url,is_vip,vip_expire_time,gender,birthday,intro,certification_type,certification_status,status,create_time,update_time,is_deleted
	</sql>
	
	<sql id="findPageWhere">
		<where>	      				
	       <if test="vo.phone != null and vo.phone != ''">
				and phone like CONCAT('%',#{vo.phone},'%')
			</if>
	       <if test="vo.nickname != null and vo.nickname != ''">
				and nickname like CONCAT('%',#{nickname},'%')
			</if>
	       <if test="vo.createTimeBegin != null and vo.createTimeBegin != ''">
				and create_time >= #{vo.createTimeBegin}
		   </if>
		   <if test="vo.createTimeEnd != null and vo.createTimeEnd != ''">
				and create_time &lt;= #{vo.createTimeEnd}
		   </if>
			and is_deleted = 0
		</where>
	</sql>
    
    <select id="selectPage" resultMap="userInfoMap">
    	select <include refid="columns" />
	    from user_info
		<include refid="findPageWhere"/>
		order by id desc
    </select>
	<select id="isCollect" resultType="com.lsj.tingshu.model.user.UserCollect">
        SELECT *
        FROM user_collect
        WHERE user_collect.user_id = #{userId}
          AND user_collect.track_id = #{trackId}
          AND is_deleted = 0
    </select>
	<select id="isSubscribe" resultType="com.lsj.tingshu.model.user.UserSubscribe">
        SELECT *
        FROM user_subscribe
        WHERE user_subscribe.user_id = #{userId}
          AND user_subscribe.album_id = #{albumId}
          AND is_deleted = 0
    </select>

</mapper>

