<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lsj.tingshu.album.mapper.TrackStatMapper">
    <update id="updateTrackStatNum">
        UPDATE track_stat
        SET track_stat.stat_num = track_stat.stat_num + #{count}
        WHERE track_stat.track_id = #{trackId}
          AND track_stat.stat_type = #{statType}
    </update>
</mapper>

