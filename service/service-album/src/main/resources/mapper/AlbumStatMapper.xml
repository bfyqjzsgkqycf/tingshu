<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lsj.tingshu.album.mapper.AlbumStatMapper">
    <update id="updateAlbumStatNum">
        UPDATE album_stat
        SET album_stat.stat_num = album_stat.stat_num + #{count}
        WHERE album_stat.album_id = #{albumId}
        AND album_stat.stat_type = #{statType}
    </update>
</mapper>

